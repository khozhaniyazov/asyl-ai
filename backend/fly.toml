app = "asyl-ai-backend"
primary_region = "fra"

[build]

[deploy]
  release_command = "python -c \"from app.db.database import init_db; import asyncio; asyncio.run(init_db())\""

[run]
  concurrency = "25"
  scaling_max = 1

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1

[mounts]
  source = "asyl_data"
  destination = "/data"

[env]
  PORT = "8080"
  ENVIRONMENT = "production"
  DATABASE_URL = "sqlite+aiosqlite:///data/asyl_ai.db"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 0
